# Consolidation Summary

This repo now targets one local path: `live.py` via `run.sh`.

```bash
./run.sh --source data/source_face.webm --camera 1
```

The legacy DOT app, GUI, Docker, batch processors, duplicate vendor checkouts, and generated outputs are intentionally removed from the supported surface.
